# Backlog — Small Improvements & Bug Fixes

Items too small for a numbered iteration but worth tracking.

Nothing currently tracked — the previous two items (TR CSV empty-amount rows silently skipped, import dialog "Bank" labeling) shipped in 0.0.2 alpha.
