[tool.poetry]
package-mode = false

[project]
name = "my-private-finances-api"
version = "0.1.0"
description = "Local, privacy-first finance backend"
authors = [
    {name = "Wissmann, Johannes",email = "johannes@wissmann.dev"}
]
license = "MIT"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "fastapi (>=0.128.0,<1.0.0)",
    "uvicorn[standard] (>=0.40.0,<1.0.0)",
    "pydantic (>=2.13.5,<3.0.0)",
    "sqlmodel (>=0.0.31,<0.1.0)",
    "sqlalchemy (>=2.0.52,<3.0.0)",
    "aiosqlite (>=0.22.1,<1.0.0)",
    "scikit-learn (>=1.9.0,<2.0.0)",
    "joblib (>=1.5.3,<2.0.0)",
    "python-multipart (>=0.0.21,<1.0.0)",
    "watchdog (>=6.0.0,<7.0.0)",
    "pydantic-settings (>=2.7.0,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)",
    "httpx (>=0.28.1,<1.0.0)",
    "ruff (>=0.14.10,<0.15.0)",
    "mypy (>=1.20.2,<2.0.0)",
    "pip-licenses (>=5.5.0,<6.0.0)",
    "alembic (>=1.19.2,<2.0.0)",
    "pytest-asyncio (>=1.4.0,<2.0.0)",
    "pytest-cov (>=7.0.0,<8.0.0)"
]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
# Ruff is the sole Python linter and formatter for this project — no black,
# isort, or flake8. Rule "I" provides import sorting (the isort replacement);
# "E"/"F" (pycodestyle + pyflakes) are on by default.
extend-select = ["I"]

[[tool.mypy.overrides]]
# scikit-learn and joblib ship no type information. Configured here rather than
# with fragile per-import `# type: ignore` comments that Ruff's formatter can
# detach from the line mypy reports.
module = ["sklearn.*", "joblib.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
